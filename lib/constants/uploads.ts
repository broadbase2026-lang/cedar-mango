/** Max size for a single image upload (bytes). */
export const MAX_IMAGE_UPLOAD_BYTES = 5 * 1024 * 1024;

export const MAX_IMAGES_PER_PRESS_RELEASE = 20;

/** Trial workspaces: images attached to a single press release. */
export const MAX_TRIAL_IMAGES_PER_PRESS_RELEASE = 5;
